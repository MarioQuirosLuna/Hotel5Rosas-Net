@model Entities_Hotel_5_Rosas.Entity_Galeria

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Entity_Galeria</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PK_Galeria" />
            <div class="form-group">
                <label asp-for="FK_Pagina" class="control-label"></label>
                <input asp-for="FK_Pagina" class="form-control" />
                <span asp-validation-for="FK_Pagina" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
