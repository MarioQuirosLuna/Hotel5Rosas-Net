@model IEnumerable<Entities_Hotel_5_Rosas.Entity_Galeria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FK_Pagina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FK_Pagina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PK_Galeria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PK_Galeria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PK_Galeria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
