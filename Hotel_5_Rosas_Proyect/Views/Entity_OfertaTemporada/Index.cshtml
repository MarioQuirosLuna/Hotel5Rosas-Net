@model IEnumerable<Entities_Hotel_5_Rosas.Entity_OfertaTemporada>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FK_Temporada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FK_Tipo_Habitacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oferta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FK_Temporada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FK_Tipo_Habitacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oferta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PK_Oferta_Temporada">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PK_Oferta_Temporada">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PK_Oferta_Temporada">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
