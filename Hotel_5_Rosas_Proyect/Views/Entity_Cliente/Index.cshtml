@model IEnumerable<Entities_Hotel_5_Rosas.Entity_Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FK_Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Numero_Tarjeta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Correo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FK_Usuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Numero_Tarjeta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Correo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PK_Cliente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PK_Cliente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PK_Cliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
