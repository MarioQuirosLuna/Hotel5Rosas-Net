@model IEnumerable<Entities_Hotel_5_Rosas.Entity_Temporada>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_Inicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_Fin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Inicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Fin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PK_Temporada">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PK_Temporada">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PK_Temporada">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
